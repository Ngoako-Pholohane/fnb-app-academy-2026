# Unit 2 – Manipulating Strings

## Overview

This unit introduces techniques for working with and manipulating text in Python.

## What I Learned

- String methods
- `.upper()`
- `.lower()`
- `.title()`
- `.strip()`
- `.replace()`
- `.find()`
- `.split()`
- `len()`
- String indexing
- String slicing
- String concatenation
- f-strings

## Practical Task: Username and Message Formatter

I created a Python program that collects a user's name and bio, then formats and manipulates the text using different string methods.

The program uses:

- String methods
- String concatenation
- `len()`
- `.strip()`
- `.replace()`
- `.lower()`
- `.title()`
- f-strings

**File:** `string_formatter.py`

## Status

Completed
